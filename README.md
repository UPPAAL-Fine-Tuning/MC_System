# MC_System
This repository implements a formal model-based approach for hyperparameter tuning in machine learning classifiers. The method formulates the search as an adaptive control problem and provides algorithms, datasets, benchmarks, and experimental settings to reproduce the reported results.
